/**
 * @jsx React.DOM
 */

'use strict';

var React = require('react');

var Nav = React.createClass({

render: function() {
  return (
    <nav>
      Nav has been rendered
    </nav>
  );
}

});

module.exports = Nav;